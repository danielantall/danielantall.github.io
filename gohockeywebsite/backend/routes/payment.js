const express = require('express');
const router = express.Router();

const { /*fill in with functions from controller file */} = require('../controllers/paymentController');

//list CRUD ops

module.exports = router;