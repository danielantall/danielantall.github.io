const express = require('express');
const router = express.Router();


const { /*fill in with functions from controller file */} = require('../controllers/guardianController');

//list CRUD ops

module.exports = router;