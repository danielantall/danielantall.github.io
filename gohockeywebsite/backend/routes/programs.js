const express = require('express');
const router = express.Router();

const { /*fill in with functions controller file*/} = require('../controllers/programsController');

//list CRUD ops

module.exports = router;